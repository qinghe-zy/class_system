# 阶段交付记录

> 按“环境 -> 数据库 -> 后端 -> 前端 -> 权限 -> 业务 -> 联调 -> 测试 -> 文档”顺序推进。

## 第一阶段：环境检查与技术方案确认

1. 已完成内容
- 检查 JDK、Maven、Node、npm、MySQL、端口、目录状态
- 确认目录为空，从0开始搭建

2. 变更文件列表
- 无代码文件

3. 运行方式
- 通过命令行执行 `java -version`、`mvn -v`、`node -v`、`mysql -e`

4. 验证结果
- JDK 21、Maven 3.9.12、Node 24.13.1、MySQL 8.0.42 可用

5. 遗留问题
- Maven/npm 默认缓存目录权限受限，后续通过提权或本地替代方案处理

6. 下一阶段计划
- 完成数据库设计与初始化

## 第二阶段：数据库设计与初始化

1. 已完成内容
- 设计并创建16张业务表
- 建立索引与唯一约束
- 初始化演示数据

2. 变更文件列表
- `sql/schema.sql`
- `sql/data.sql`

3. 运行方式
- `mysql -uroot -p123456 < sql/schema.sql`
- `mysql -uroot -p123456 < sql/data.sql`

4. 验证结果
- 数据库表创建成功，用户和行为记录均有初始数据

5. 遗留问题
- 无

6. 下一阶段计划
- 后端工程与基础框架搭建

## 第三阶段：后端基础工程搭建

1. 已完成内容
- Spring Boot + MyBatis Plus + JWT + Knife4j
- 统一返回、全局异常、分页、拦截器鉴权

2. 变更文件列表
- `backend/pom.xml`
- `backend/src/main/java/**`
- `backend/src/main/resources/application.yml`

3. 运行方式
- `cd backend && mvn spring-boot:run`

4. 验证结果
- `mvn -DskipTests compile` 通过

5. 遗留问题
- 初次联调发现JWT密钥长度问题，已修复

6. 下一阶段计划
- 完成前端基础工程

## 第四阶段：前端基础工程搭建

1. 已完成内容
- Vue3 + Vite + Element Plus + Pinia + Router + Axios + ECharts
- 公共布局与路由权限框架

2. 变更文件列表
- `frontend/package.json`
- `frontend/vite.config.js`
- `frontend/src/**`

3. 运行方式
- `cd frontend && npm install && npm run dev`

4. 验证结果
- `npm run build` 成功
- `npm run dev` 启动成功（提权环境）

5. 遗留问题
- 受沙箱限制，本地调试启动需允许 npm 子进程

6. 下一阶段计划
- 完成登录认证与权限控制

## 第五阶段：登录认证与权限控制

1. 已完成内容
- 登录接口、JWT签发解析
- 接口角色权限校验
- 前端路由角色校验

2. 变更文件列表
- `AuthService`、`AuthInterceptor`、`router/index.js`

3. 运行方式
- 登录后携带 Bearer token 请求接口

4. 验证结果
- 登录成功返回 token

5. 遗留问题
- 无

6. 下一阶段计划
- 用户管理模块

## 第六阶段：用户管理模块

1. 已完成内容
- 管理员用户增删改查、分页、搜索、重置密码、启停

2. 变更文件列表
- `UserService`
- `AdminUserController`
- `AdminUsers.vue`

3. 运行方式
- 管理员登录后访问 `/admin/users`

4. 验证结果
- 列表查询与操作可执行

5. 遗留问题
- 无

6. 下一阶段计划
- 课程管理与审核

## 第七阶段：课程管理与课程审核模块

1. 已完成内容
- 教师课程CRUD
- 管理员课程审核
- 审核记录落库

2. 变更文件列表
- `CourseService`
- `TeacherCourseController`、`AdminCourseController`
- `TeacherCourses.vue`、`AdminCourseAudit.vue`

3. 运行方式
- 教师端创建课程，管理员端审核

4. 验证结果
- 课程状态可从 `PENDING` 切换到 `APPROVED/REJECTED`

5. 遗留问题
- 无

6. 下一阶段计划
- 选课与课堂会话

## 第八阶段：学生选课与课堂会话模块

1. 已完成内容
- 学生选课、我的课程
- 教师创建课堂会话、管理员总览

2. 变更文件列表
- `SessionService`
- `StudentCourseController`、`SessionController`
- `StudentCourseList.vue`、`TeacherSessions.vue`

3. 运行方式
- 学生端选课，教师端创建会话

4. 验证结果
- 会话可创建并按角色可见

5. 遗留问题
- 无

6. 下一阶段计划
- 签到与测验

## 第九阶段：签到与测验模块

1. 已完成内容
- 签到任务创建与学生签到
- 测验创建、题目管理、提交自动评分

2. 变更文件列表
- `SignInService`、`QuizService`
- `SignInController`、`QuizController`
- `TeacherSignin.vue`、`TeacherQuiz.vue`、`StudentSignin.vue`、`StudentQuiz.vue`

3. 运行方式
- 教师创建任务/测验，学生执行签到与提交

4. 验证结果
- 提交记录与得分可查询

5. 遗留问题
- 题型目前以基础单选为主

6. 下一阶段计划
- 行为检测核心模块

## 第十阶段：行为检测核心模块

1. 已完成内容
- 行为采集上报
- 规则引擎分析
- 检测记录落库
- 三端按权限查询

2. 变更文件列表
- `BehaviorCollectService`
- `BehaviorAnalyzeService`
- `BehaviorRuleEngine`
- `BehaviorRecordService`
- `BehaviorController`
- `StudentClassroom.vue`

3. 运行方式
- 学生端在线课堂页开启上报

4. 验证结果
- 上报成功返回 `recordId`
- 教师端可查询新增检测记录

5. 遗留问题
- ML/视频识别为预留扩展点，当前为规则引擎

6. 下一阶段计划
- 统计分析模块

## 第十一步：统计分析模块

1. 已完成内容
- 管理员/教师/学生统计接口
- ECharts 图表展示

2. 变更文件列表
- `StatsService`
- `StatsController`
- `AdminStats.vue`、`TeacherStats.vue`、`StudentStats.vue`

3. 运行方式
- 三端进入统计页自动加载图表

4. 验证结果
- 管理员总览与趋势、教师分布、学生活跃趋势均可返回真实数据

5. 遗留问题
- 无

6. 下一阶段计划
- 前后端联调与测试

## 第十二阶段：前后端联调

1. 已完成内容
- 登录、统计、行为上报、行为列表等关键链路联调

2. 变更文件列表
- 配置修复：`application.yml` JWT密钥

3. 运行方式
- 启动后端后，通过前端页面或接口调用联调

4. 验证结果
- 登录接口成功
- 行为上报成功
- 统计接口成功

5. 遗留问题
- 无

6. 下一阶段计划
- 测试修复与文档收敛

## 第十三阶段：测试、修复与优化

1. 已完成内容
- 修复 UTF-8 BOM 编译问题
- 修复 JWT 密钥长度问题
- 处理 npm/maven 权限限制并完成构建验证

2. 变更文件列表
- 全量源码编码修复
- `application.yml`

3. 运行方式
- `mvn compile`、`npm run build`

4. 验证结果
- 后端编译通过
- 前端构建通过

5. 遗留问题
- 开发服务器启动在受限环境需要放开子进程权限

6. 下一阶段计划
- 完成文档交付

## 第十四阶段：文档整理与交付

1. 已完成内容
- README、数据库说明、行为检测说明、接口说明、软件开发说明书、阶段记录

2. 变更文件列表
- `README.md`
- `docs/数据库设计说明.md`
- `docs/行为检测逻辑说明.md`
- `docs/接口文档说明.md`
- `docs/软件开发说明书.md`
- `docs/阶段交付记录.md`

3. 运行方式
- 按 README 执行

4. 验证结果
- 文档与代码一致，支撑课程设计答辩

5. 遗留问题
- 可选扩展：AI模型、视频识别、告警推送

6. 下一阶段计划
- 可进入演示与答辩阶段

## 第十五阶段：联调问题修复（签到惩罚/测验同步/课堂内容附件）

1. 已完成内容
- 签到新增“过期不允许签到”惩罚机制，自动落库 `sign_in_penalty`
- 教师支持上传 Word/PPT/PDF 并发布课堂内容，管理员审核后学生可见附件
- 测验改为“列表页 + 独立编辑页”，支持新界面编辑题目/选项
- 学生测验列表增加时间状态（未开始/进行中/已结束）并修复发布同步问题
- 修复时间提交时区偏移：前端统一提交 `yyyy-MM-ddTHH:mm:ss`
- 填空题交互优化：前端必填校验，后端支持多参考答案（`|` 分隔）

2. 变更文件列表
- `backend/src/main/java/com/ocms/backend/service/SignInService.java`
- `backend/src/main/java/com/ocms/backend/service/QuizService.java`
- `backend/src/main/java/com/ocms/backend/controller/QuizController.java`
- `backend/src/main/java/com/ocms/backend/util/FileStorageService.java`
- `backend/src/main/java/com/ocms/backend/config/WebMvcConfig.java`
- `frontend/src/views/teacher/TeacherQuiz.vue`
- `frontend/src/views/teacher/TeacherQuizEditor.vue`
- `frontend/src/views/student/StudentQuiz.vue`
- `frontend/src/views/teacher/TeacherSignin.vue`
- `frontend/src/views/teacher/TeacherSessions.vue`
- `frontend/src/views/student/StudentSignin.vue`
- `frontend/src/views/student/StudentCourseContent.vue`
- `frontend/src/router/index.js`
- `frontend/src/api/index.js`
- `frontend/src/utils/datetime.js`
- `sql/schema.sql`
- `sql/data.sql`
- `sql/migration_20260318_force.sql`

3. 运行方式
- 后端：`cd backend && mvn spring-boot:run`
- 前端：`cd frontend && npm run dev`
- 迁移：`mysql --force -uroot -p123456 < sql/migration_20260318_force.sql`

4. 验证结果
- 后端编译通过：`mvn -q -DskipTests compile`
- 前端构建通过：`npm run build`
- 上传接口通过：`POST /api/teacher/files/upload` 返回附件URL
- 测验同步通过：教师发布后学生可见且状态为 `ONGOING`
- 过期签到惩罚通过：学生签到返回禁止，惩罚记录可查

5. 遗留问题
- 控制台在当前终端环境下中文响应可能出现编码显示异常（不影响业务）

6. 下一阶段计划
- 可直接进行网页演示与答辩
