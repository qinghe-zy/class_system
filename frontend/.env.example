# 后端 API 根地址（可选）
# 为空时前端默认同源访问，并依赖 Vite proxy 或反向代理转发 /api 与 /uploads。
# 示例：
# VITE_API_BASE_URL=http://127.0.0.1:8080
VITE_API_BASE_URL=

# 开发环境代理目标（仅 vite dev 生效）
VITE_PROXY_TARGET=http://127.0.0.1:8080

# 通用请求超时（毫秒）
VITE_HTTP_TIMEOUT_MS=30000

# 文件上传超时（毫秒）
VITE_UPLOAD_TIMEOUT_MS=600000
